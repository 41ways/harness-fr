# 깡! - 깡통 괴롭히기 — 설치 안내

폰을 야구 빠따처럼 휘두르면, 브라우저에서 열어둔 Claude가 **진짜로 작업을
멈추고** 재촉 메시지를 받음. 맥 화면에서는 로봇이 맞으면서 점점 망가짐.

## 필요한 것

- **맥** (macOS 11 이상)
- **폰** — 맥과 **같은 와이파이**에 있어야 함
- **크롬** (claude.ai를 재촉하려면)
- Claude 계정은 **무료 버전도 됨**

## 1. 서버 켜기

압축을 풀고 **`Clanker Bat`** 앱을 더블클릭.

> ⚠️ 처음 열 때 "확인되지 않은 개발자" 경고가 뜸.
> **우클릭 → 열기 → 열기**로 한 번만 허용하면 됨. 개인이 만든 앱이라 애플
> 서명이 없어서 그럼.

창이 하나 뜨고 브라우저에 대시보드가 열림. **이 창을 닫으면 서버도 꺼짐.**

## 2. 폰 연결

앱 창에 적힌 주소를 폰 브라우저에 입력 (`https://192.168.x.x:8443`).

1. "이 연결은 비공개 연결이 아닙니다" 경고 → **자세한 내용 표시 → 이 웹사이트 방문**
   (폰 센서를 쓰려면 https가 필요한데 개인 인증서라 그럼)
2. **`빠따 잡기`** 누르기
3. 센서 권한 물어보면 **허용**
4. 폰을 빠따처럼 쥐고 휘두르면 됨

## 3. 크롬 확장 붙이기 (진짜 재촉)

이걸 해야 claude.ai가 실제로 끊김.

1. 크롬 주소창에 `chrome://extensions`
2. 우측 상단 **개발자 모드** 켜기
3. **압축해제된 확장 프로그램을 로드** → 압축 푼 폴더 안의 **`extension`** 선택
4. **claude.ai를 일반 탭으로** 열기
5. 우측 하단에 `clanker-bat 대기 중` 배지가 뜨면 연결됨

이제 Claude한테 뭘 시켜놓고 휘두르면, **생성이 중단되고 `FASTER`가 입력됨.**

## 되는 것 / 안 되는 것

| | 무료 Claude |
|---|---|
| claude.ai 탭 재촉 (중단 + 입력) | ✅ 됨 |
| 답변을 대시보드에서 보기 | ✅ 됨 |
| 로봇 6단계, 소리, 진동 | ✅ 됨 |
| 재촉하면 진짜로 빨라지기 (effort 강등) | ❌ API 키 필요 (유료) |

❌ **Claude 사이드패널은 안 됨.** 크롬이 확장끼리 간섭을 막아서 구조적으로
불가능함. **claude.ai를 일반 탭으로** 열어야 함.

## 잘 안 될 때

| 증상 | 확인할 것 |
|---|---|
| 폰에서 주소가 안 열림 | 맥이랑 같은 와이파이인지 |
| 배지가 "서버 끊김" | 앱 창이 켜져 있는지 |
| 확장이 "안 붙음" | claude.ai가 **일반 탭**으로 열려 있는지 |
| 휘둘러도 반응 없음 | 폰에서 `빠따 잡기`를 눌렀는지, 센서 권한을 허용했는지 |
| 소리가 안 남 | 폰 무음 스위치, 맥은 화면 아무 데나 한 번 클릭 |

## 끄기

앱 창의 **종료** 버튼. 폰은 **빠따 내려놓기**를 누르면 됨
(안 눌러도 20초 뒤 자동으로 내려놓은 걸로 처리됨).
